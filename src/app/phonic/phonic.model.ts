export class Phonic {
  word: string;
  meta: string;
  file: string;

  constructor(word: string, meta: string, file: string) {
    this.word = word;
    this.meta = meta;
    this.file = file;
  }
}
