export class Phonic {
  word: string;
  meta: string;
  file: string;

  constructor(word: string, file: string, meta: string) {
    this.word = word;
    this.file = file;
    this.meta = meta;
  }
}
